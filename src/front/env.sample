# API URL - Gateway address
VITE_API_URL=http://localhost:8080

# Default login credentials (for development only)
VITE_DEFAULT_EMAIL=admin@pbx.local
VITE_DEFAULT_PASSWORD=Agent666

# App mode: "full" (all features) or "delta" (Delta CRM only - no recruitment/projects, expanded menu)
VITE_APP_MODE=full

# Application name (shown in header, login page, browser title)
VITE_APP_NAME=PBX by Agent
